🚢 -Marine-Asset-Tracker
Track marine vessels, equipment &amp; maintenance logs.

## Backend

02-06-26 Report

- Basic bug tracker backend scaffold
- Express server with `GET /` returning `Hello World!`
- Uses `dotenv` to load environment variables from `.env`
- `package.json` includes `start` and `dev` scripts with `nodemon`
- Prepared for further API development and role-based access features

## Frontend
02-06-26 Report

- Vite + React frontend scaffold
- Tailwind CSS installed for styling
- App entry points at `src/main.jsx` and `src/App.jsx`
- `npm run dev` starts the Vite dev server
- `npm run build` creates the production bundle
